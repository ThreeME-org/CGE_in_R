install.packages(pkgs = "src/github_packages/pegr_0.0.2.tar.gz", 
                 repos = NULL,
                 type = "source"
)

install.packages(pkgs = "src/github_packages/ofce_1.3.4.tar.gz", 
                 repos = NULL,
                 type = "source"
)

install.packages(pkgs = "src/github_packages/tresthor_1.2.0.tar.gz", 
                 repos = NULL,
                 type = "source"
)

install.packages(pkgs = "src/github_packages/ermeeth_0.3.05.7.tar.gz", 
                 repos = NULL,
                 type = "source"
)